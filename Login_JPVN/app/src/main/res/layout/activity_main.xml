<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        LinearLayout vertical: apila los elementos uno debajo del otro.
        Está anclado a los 4 lados del padre con los constraints.
        gravity="center" centra todo el contenido en pantalla.
    -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Imagen decorativa de perfil -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="24dp"
            android:src="@drawable/login" />

        <!-- Etiqueta del campo usuario -->
        <TextView
            android:id="@+id/textView2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Usuario:" />

        <!--
            inputType="textEmailAddress" muestra teclado con @ automáticamente.
            El id "correo" lo usaremos en Kotlin para leer lo que escribe el usuario.
        -->
        <EditText
            android:id="@+id/correo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:hint="mail@mail.com"
            android:inputType="textEmailAddress"
            android:minHeight="48dp" />

        <!-- Etiqueta del campo contraseña -->
        <TextView
            android:id="@+id/textView3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Contraseña:" />

        <!--
            inputType="textPassword" oculta los caracteres con puntos.
        -->
        <EditText
            android:id="@+id/password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:hint="password"
            android:inputType="textPassword"
            android:minHeight="48dp" />

        <!--
            El id "login" lo usaremos en Kotlin para
            asignarle la acción de autenticación con Firebase.
        -->
        <Button
            android:id="@+id/login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="#4E60FE"
            android:text="Iniciar" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>